
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  name: {{ template "nginx-static.fullname" . }}-html-pvc
spec:
  accessModes:
  - ReadOnlyMany
  # For the deploy job
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}